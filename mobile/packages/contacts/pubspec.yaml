name: ente_contacts
description: Shared contacts client, cache, and Rust orchestration for Ente apps
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  ente_rust:
    path: ../rust
  flutter:
    sdk: flutter
  logging: ^1.2.0
  path: ^1.9.1
  path_provider: ^2.1.5
  shared_preferences: ^2.2.3
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.3.6

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
