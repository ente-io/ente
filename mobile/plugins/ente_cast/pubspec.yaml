name: ente_cast
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  collection:
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.5
  stack_trace:

dev_dependencies:
  flutter_lints:

flutter:
