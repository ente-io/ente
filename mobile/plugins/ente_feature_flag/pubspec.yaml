name: ente_feature_flag
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  collection:
  dio:
  shared_preferences:
  stack_trace:

dev_dependencies:
  flutter_lints:

flutter: