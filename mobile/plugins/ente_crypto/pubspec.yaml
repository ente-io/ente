name: ente_crypto
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  collection:
  computer:
    git: "https://github.com/ente-io/computer.git"
  flutter:
    sdk: flutter
  flutter_sodium: ^0.2.0
  logging: ^1.3.0
  
dev_dependencies:
  flutter_lints:

flutter: