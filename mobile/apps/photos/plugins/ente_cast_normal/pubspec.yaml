name: ente_cast_normal
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  cast:
    git:
      url: https://github.com/guyluz11/flutter_cast.git
      ref: multicast_version
      commit: 1f39cd4
  ente_cast:
    path: ../ente_cast
  flutter:
    sdk: flutter
  stack_trace:

dev_dependencies:
  flutter_lints:

flutter:
