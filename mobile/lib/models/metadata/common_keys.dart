const magicKeyVisibility = 'visibility';

// Visibility Constants
const visibleVisibility = 0;
const archiveVisibility = 1;

///Do not use [hiddenVisibility] for hidden files
const hiddenVisibility = 2;
