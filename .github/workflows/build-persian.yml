name: Build Ente Persian APK

on:
  push:
    branches: [ persian-localized ]
  workflow_dispatch:

env:
  FLUTTER_VERSION: "3.27.1"
  JAVA_VERSION: "17"
  API_ENDPOINT: "https://api.photos.degoogle.ir"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo apt-get clean
          df -h

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: "stable"
          cache: true

      - name: Install Rust (Stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,x86_64-linux-android

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: mobile/apps/photos/rust

      # ============================================
      # بررسی وجود فایل‌های Rust
      # ============================================
      - name: Check if Rust files exist
        id: check_rust
        run: |
          if [ -d "mobile/apps/photos/lib/src/rust" ]; then
            echo "rust_exists=true" >> $GITHUB_OUTPUT
            echo "✅ Rust files already exist"
          else
            echo "rust_exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ Need to generate Rust bindings"
          fi

      - name: Install flutter_rust_bridge_codegen
        if: steps.check_rust.outputs.rust_exists == 'false'
        run: cargo install flutter_rust_bridge_codegen --version 1.82.6 --locked

      - name: Generate Rust bindings
        if: steps.check_rust.outputs.rust_exists == 'false'
        working-directory: mobile/apps/photos
        run: |
          # استفاده از CLI بدون config file
          echo "🔨 Generating Rust bindings with CLI parameters"
          flutter_rust_bridge_codegen \
            --rust-input rust/src/api.rs \
            --dart-output lib/src/rust \
            --rust-crate-dir rust

      # ============================================
      # Fix Dependencies
      # ============================================
      - name: Check and fix dependencies
        run: |
          cd mobile/apps/photos

          # بررسی نسخه mockito
          MOCKITO_VERSION=$(grep "mockito:" pubspec.yaml | grep -oP '\d+\.\d+\.\d+' || echo "not_found")
          echo "Current mockito version: $MOCKITO_VERSION"

          # اگه mockito >= 5.4.6 بود، downgrade کن
          if [[ "$MOCKITO_VERSION" > "5.4.5" ]] || [[ "$MOCKITO_VERSION" == "not_found" ]]; then
            echo "⚠️ Downgrading mockito to 5.4.5 for Dart compatibility"
            sed -i '/dev_dependencies:/,/^[^ ]/ s/mockito: .*/  mockito: 5.4.5/' pubspec.yaml
          fi

          echo "📦 Final pubspec.yaml dependencies:"
          grep -A 5 "dev_dependencies:" pubspec.yaml

      - name: Flutter doctor
        run: flutter doctor -v

      - name: Generate l10n
        run: flutter gen-l10n
        working-directory: mobile/apps/photos

      - name: Get dependencies
        run: flutter pub get
        working-directory: mobile/apps/photos

      - name: Clean build
        run: flutter clean
        working-directory: mobile/apps/photos

      # ============================================
      # Build APK
      # ============================================
      - name: Build APK
        run: |
          flutter build apk \
            --release \
            --flavor independent \
            --dart-define=endpoint=${{ env.API_ENDPOINT }} \
            --dart-define=cronetHttpNoPlay=true \
            --no-tree-shake-icons
        working-directory: mobile/apps/photos

      - name: Rename and organize APK
        run: |
          cd mobile/apps/photos/build/app/outputs/flutter-apk
          APK_NAME="ente-persian-$(date +%Y%m%d-%H%M%S).apk"
          mv app-independent-release.apk "$APK_NAME"
          sha256sum "$APK_NAME" > checksums.txt

          echo "✅ Build completed successfully!"
          echo "📦 APK: $APK_NAME"
          cat checksums.txt

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ente-persian-build
          path: |
            mobile/apps/photos/build/app/outputs/flutter-apk/*.apk
            mobile/apps/photos/build/app/outputs/flutter-apk/checksums.txt
          retention-days: 90

