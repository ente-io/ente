name: "Lint (ensu desktop)"

on:
    # Run on every pull request (open or push to it) that changes ensu desktop
    pull_request:
        paths:
            - "rust/apps/ensu/**"
            - "rust/core/**"
            - "rust/ensu/**"
            - ".github/workflows/ensu-desktop-lint.yml"

permissions:
    contents: read

# Cancel in-progress lint runs when a new commit is pushed.
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

env:
    RUSTFLAGS: -D warnings

jobs:
    lint:
        runs-on: ubuntu-22.04
        defaults:
            run:
                working-directory: rust/apps/ensu/src-tauri
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Cache
              uses: actions/cache@v4
              with:
                  path: |
                      ~/.cargo/registry
                      ~/.cargo/git
                      rust/apps/ensu/src-tauri/target
                  key: ${{ runner.os }}-cargo-ensu-desktop-${{ hashFiles('rust/apps/ensu/src-tauri/Cargo.lock') }}

            - name: Install system dependencies
              run: |
                  sudo apt-get update
                  sudo apt-get install -y libwebkit2gtk-4.0-dev libssl-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

            - name: Fetch Rust dependencies
              run: cargo fetch

            - name: Patch llama.cpp mtmd sources
              shell: bash
              working-directory: .
              run: rust/ensu/inference/tool/patch_llama_mtmd.sh

            - name: Ensure Tauri distDir exists for macro checks
              run: mkdir -p ../../../../web/apps/ensu/out

            - run: cargo fmt --check

            - run: cargo clippy --all-targets --all-features

            - run: cargo build
