# Copy this file into `.env.local` or `.env.development.local` and uncomment
# these to develop against apps and server running on localhost.
#
# For details, please see `apps/photos/.env.development` and `apps/albums/.env`.

#NEXT_PUBLIC_ENTE_ENDPOINT = http://localhost:8080
#NEXT_PUBLIC_ENTE_ALBUMS_ENDPOINT = http://localhost:3002
#NEXT_PUBLIC_ENTE_PHOTOS_ENDPOINT = http://localhost:3000
