import { ActivityIndicator } from "@/base/components/mui/ActivityIndicator";

export const SmallLoadingSpinner = () => (
    <ActivityIndicator
        style={{
            width: "20px",
            height: "20px",
        }}
    />
);
