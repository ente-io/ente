import EnteSpinner from "@ente/shared/components/EnteSpinner";

export const SmallLoadingSpinner = () => (
    <EnteSpinner
        style={{
            width: "20px",
            height: "20px",
        }}
    />
);
