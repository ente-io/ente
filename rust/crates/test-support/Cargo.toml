[package]
name = "ente-test-support"
edition = "2024"
description = "Shared integration test fixture that runs a local Museum backed by PGlite"
publish = false

[dependencies]
serde_json = "1.0"
uuid = { version = "1.11", features = ["v4"] }
